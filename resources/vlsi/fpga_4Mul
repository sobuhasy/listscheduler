/////////////////////////////////////////////////////////
// Resource Constraints                                //
// Listing all available resources                     //
/////////////////////////////////////////////////////////
// Usage: 
// ResNameNum 	Operation1 Operation2     ...
// Available operations are ADD, SUB, DIV, MULT, SH, AND, OR, MEM, CMP, OTHER

RES00		OTHER ADD SUB SH AND OR CMP
RES01		OTHER ADD SUB SH AND OR CMP
RES02		OTHER ADD SUB SH AND OR CMP
RES03		OTHER ADD SUB SH AND OR CMP
RES04		OTHER ADD SUB SH AND OR CMP
RES05		OTHER ADD SUB SH AND OR CMP
RES06		OTHER ADD SUB SH AND OR CMP
RES07		OTHER ADD SUB SH AND OR CMP
RES08		OTHER ADD SUB SH AND OR CMP
RES09		OTHER ADD SUB SH AND OR CMP
RES10		OTHER ADD SUB SH AND OR CMP
RES11		OTHER ADD SUB SH AND OR CMP
RES12		OTHER ADD SUB SH AND OR CMP
RES13		OTHER ADD SUB SH AND OR CMP
RES14		OTHER ADD SUB SH AND OR CMP
RES15		OTHER ADD SUB SH AND OR CMP
RES16		OTHER ADD SUB SH AND OR CMP
RES17		OTHER ADD SUB SH AND OR CMP
RES18		OTHER ADD SUB SH AND OR CMP
RES19		OTHER ADD SUB SH AND OR CMP
RES20		OTHER ADD SUB SH AND OR CMP
RES21		OTHER ADD SUB SH AND OR CMP
RES22		OTHER ADD SUB SH AND OR CMP
RES23		OTHER ADD SUB SH AND OR CMP
RES24		OTHER ADD SUB SH AND OR CMP
RES25		OTHER ADD SUB SH AND OR CMP
RES26		OTHER ADD SUB SH AND OR CMP
RES27		OTHER ADD SUB SH AND OR CMP
RES28		OTHER ADD SUB SH AND OR CMP
RES29		OTHER ADD SUB SH AND OR CMP
RES30		OTHER ADD SUB SH AND OR CMP
RES31		OTHER ADD SUB SH AND OR CMP
RES32		OTHER ADD SUB SH AND OR CMP
RES33		OTHER ADD SUB SH AND OR CMP
RES34		OTHER ADD SUB SH AND OR CMP
RES35		OTHER ADD SUB SH AND OR CMP
RES36		OTHER ADD SUB SH AND OR CMP
RES37		OTHER ADD SUB SH AND OR CMP
RES38		OTHER ADD SUB SH AND OR CMP
RES39		OTHER ADD SUB SH AND OR CMP
RES40		OTHER ADD SUB SH AND OR CMP
RES41		OTHER ADD SUB SH AND OR CMP
RES42		OTHER ADD SUB SH AND OR CMP
RES43		OTHER ADD SUB SH AND OR CMP
RES44		OTHER ADD SUB SH AND OR CMP
RES45		OTHER ADD SUB SH AND OR CMP
RES46		OTHER ADD SUB SH AND OR CMP
RES47		OTHER ADD SUB SH AND OR CMP
RES48		OTHER ADD SUB SH AND OR CMP
RES49		OTHER ADD SUB SH AND OR CMP
RES50		MEM
RES51		MEM
RES52		MULT DIV
RES53		MULT DIV
RES54		MULT DIV
RES55		MULT DIV
